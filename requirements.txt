# Core ML
torch>=2.1.0
transformers>=4.36.0
accelerate>=0.25.0
peft>=0.7.0
trl>=0.7.4
bitsandbytes>=0.41.0
datasets>=2.14.0

# Unsloth — установить отдельно:
# pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"

# Inference
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Data & validation
jsonschema>=4.20.0
